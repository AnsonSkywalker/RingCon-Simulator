# AIGC note: root CMakeLists for the PlatformIO ESP-IDF env (env:esp32classic).
# PlatformIO's Arduino envs ignore this file.
cmake_minimum_required(VERSION 3.16)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(ringcon)
